# Agave boots from ISO/USB, not PXE — drop the archiso_pxe_* + memdisk hooks
# (they only added missing-binary errors: nfsmount, ipconfig, nbd-client).
HOOKS=(base udev microcode modconf kms archiso archiso_loop_mnt block filesystems keyboard)
COMPRESSION="xz"
COMPRESSION_OPTIONS=(-9e)
