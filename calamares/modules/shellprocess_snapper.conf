# snapper + snap-pac + grub-btrfs setup on the target (docs/PLAN.md Phase 3).
# Runs in the target chroot; the script ships with agave-calamares-config.
---
dontChroot: false
timeout: 120
verbose: true
script:
    - "/usr/share/agave/calamares/agave-snapper"
